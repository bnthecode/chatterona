// const functions = require("firebase-functions");

// exports.registerUser = functions.auth.user().onCreate(event => {
//     return admin.firestore().collection('users').doc(event.data.uid).set({ name: 'testing??'})
//   });
import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#424242",
      light: "#616161"
    },
    secondary: {
      main: "#eee",
      light: '#eee'
    }
  },
});

export default theme;
